services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: money_management
      POSTGRES_USER: money
      POSTGRES_PASSWORD: money
    ports:
      - "5433:5432"
    volumes:
      - money_management_pgdata:/var/lib/postgresql/data

volumes:
  money_management_pgdata:
